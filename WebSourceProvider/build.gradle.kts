version = 1

cloudstream {
    description = "Configurable CloudStream website provider with native per-plugin settings."
    authors = listOf("webcreaters-ux")
    status = 1
    tvTypes = listOf("Others", "Movie", "TvSeries")
    iconUrl = "https://www.google.com/s2/favicons?domain=github.com&sz=%size%"

    // This plugin contains Android UI for its native settings screen.
    isCrossPlatform = false
}
